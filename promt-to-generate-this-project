I need you to review my **existing deployment README** first:

`D:\New-DATA-After-backup\grafana-key-vmagent-project\grafana-vmagent-keyclock-storage-nama.txt`

This README describes a monitoring stack that I have already deployed in one environment across **3 different machines**.

### Current requirement

I want to extend the existing deployment by adding **Keycloak** and **Traefik**.

The objective is:

**User → Traefik → Keycloak / Grafana / other services**

I want each URL/service to be accessed through Traefik so that Traefik becomes the common entry point and I can monitor and observe traffic going to all services across all 3 machines.

### Important

Do **not** immediately start giving me a generic Docker Compose example.

First:

1. Read and understand the complete README:
   `D:\New-DATA-After-backup\grafana-key-vmagent-project\grafana-vmagent-keyclock-storage-nama.txt`

2. Identify:

   * All 3 machines
   * IP addresses
   * Hostnames
   * Docker networks
   * Docker Compose projects
   * Existing containers
   * Existing URLs
   * Existing ports
   * Grafana configuration
   * vmagent configuration
   * VictoriaMetrics configuration
   * Keycloak-related configuration, if any
   * Storage configuration
   * Nginx/Apache/HAProxy/other reverse proxies, if any
   * Firewall/network requirements
   * How monitoring traffic currently flows between the 3 machines

3. Build a clear picture of the **current architecture before proposing changes**.

### Desired architecture

I want something conceptually similar to:

Internet / Users
|
v
Traefik
|
+------------------+
|                  |
v                  v
Keycloak            Grafana
|                  |
|                  |
+------ Auth ------+
|
+--> Other monitoring services

And this should work across the existing **3-machine environment**.

I need you to determine whether Traefik should run:

* On one dedicated machine
* On all 3 machines
* Or in another architecture

Do not assume the answer. Recommend the architecture based on the current deployment described in the README.

### Keycloak requirement

I want Keycloak to provide authentication/SSO for the monitoring URLs.

For example, I may eventually have URLs such as:

* `grafana.<domain>`
* `keycloak.<domain>`
* `vmagent.<domain>`
* `victoriametrics.<domain>`
* Other monitoring URLs/services

Determine which services should actually be exposed publicly through Traefik and which should remain internal.

I specifically want to avoid exposing internal monitoring APIs unnecessarily.

### Traefik requirement

Configure Traefik so that it can:

* Receive HTTP/HTTPS traffic
* Terminate TLS
* Route requests based on hostname
* Forward traffic to the correct backend
* Provide access logs
* Provide metrics
* Allow me to monitor traffic
* Show request count
* Show response status codes
* Show latency/duration
* Show source/client information where appropriate
* Provide Prometheus-compatible metrics if possible

I want the Traefik metrics to integrate with my existing monitoring stack.

### Monitoring requirement

The important part is that I want to monitor traffic across **all 3 machines**.

Determine how Traefik traffic metrics/logs should be collected and centralized.

For example:

Traefik Machine 1
|
+--> Traefik metrics
|
+--> vmagent
|
v
VictoriaMetrics
|
v
Grafana

Traefik Machine 2
|
+--> Traefik metrics
|
+--> vmagent
|
v
VictoriaMetrics

Traefik Machine 3
|
+--> Traefik metrics
|
+--> vmagent
|
v
VictoriaMetrics

But determine whether this is actually the best design based on my existing deployment.

### I also want traffic visibility

I want to be able to answer questions such as:

* How many requests are hitting each URL?
* Which URL receives the most traffic?
* Which machine is receiving the traffic?
* Which backend is generating errors?
* How many HTTP 4xx responses?
* How many HTTP 5xx responses?
* Average request latency?
* P95/P99 latency?
* Requests per second?
* Traffic by hostname?
* Traffic by HTTP method?
* Backend response time?
* Traefik errors?
* TLS errors?
* Connection errors?
* Which services are unavailable?
* Is traffic reaching Machine 1, 2, or 3?
* Are requests being load-balanced correctly?
* Are there differences in traffic between the 3 machines?

If Traefik access logs are more appropriate for some of these requirements, explain how to collect and visualize them alongside Prometheus metrics.

### Authentication architecture

Please explain how Keycloak should integrate with Traefik.

Consider appropriate approaches such as:

* ForwardAuth
* OAuth2/OIDC
* oauth2-proxy
* Traefik middleware
* Keycloak directly where appropriate

Do not blindly choose one.

Explain the security implications and recommend the architecture that fits my environment.

### URL design

Propose a clean URL structure.

For example:

`https://grafana.example.com`
`https://keycloak.example.com`

And determine whether internal services should instead use:

`https://grafana.example.com`
`https://vm.example.com`
`https://traefik.example.com`

or remain accessible only internally.

### TLS

Include:

* TLS termination
* Certificate management
* Let's Encrypt if appropriate
* Internal CA option if this is an internal environment
* Certificate renewal
* HTTP → HTTPS redirect
* Secure headers
* TLS versions/ciphers where appropriate

### High availability

Because the deployment is already spread across 3 machines, analyze whether Traefik and Keycloak need HA.

Discuss:

* Single Traefik vs 3 Traefik instances
* Single Keycloak vs multiple Keycloak instances
* Database requirements for Keycloak
* Load balancing
* VIP requirements
* DNS requirements
* Failure scenarios
* What happens if one machine goes down

Do not introduce unnecessary complexity if the existing environment does not require it.

### Firewall/networking

Based on the README, identify exactly what ports need to be opened between:

* Users → Traefik
* Traefik → Keycloak
* Traefik → Grafana
* Traefik → other services
* vmagent → exporters/services
* vmagent → VictoriaMetrics
* Keycloak → database
* Monitoring components → Traefik

Give me a table:

| Source | Destination | Port | Protocol | Purpose |
| ------ | ----------- | ---: | -------- | ------- |

Only include ports that are actually required.

### Docker/network design

Review the existing Docker networks and recommend how Traefik should connect to the existing containers.

Be careful about:

* Existing network names
* Container DNS
* Static IPs
* Published ports
* Docker labels
* Network isolation
* `expose` vs `ports`
* Whether Traefik should access containers directly over Docker networking

Do not break the existing deployment.

### Grafana integration

I want a Grafana dashboard for Traefik.

Create/recommend dashboards covering:

1. Requests/sec
2. Total requests
3. HTTP 2xx
4. HTTP 3xx
5. HTTP 4xx
6. HTTP 5xx
7. Error rate
8. Request duration
9. P50/P95/P99 latency
10. Requests by hostname
11. Requests by router
12. Requests by service/backend
13. Traffic by machine
14. Active connections
15. TLS handshakes/errors
16. Backend errors
17. Traefik uptime

Use Prometheus metric names appropriate for the installed Traefik version.

### Security

Pay particular attention to security.

I do NOT want:

* VictoriaMetrics exposed publicly
* vmagent exposed publicly
* Prometheus endpoints unnecessarily exposed
* Docker API exposed to the Internet
* Keycloak admin console exposed without protection
* Internal exporter endpoints exposed publicly
* Unauthenticated access to monitoring APIs

Explain exactly what should be externally accessible and what should remain internal.

### Deliverables

After reviewing the README, provide:

#### 1. Current architecture

Create an ASCII architecture diagram based specifically on my existing deployment.

#### 2. Proposed architecture

Create an updated architecture showing:

* 3 machines
* Traefik
* Keycloak
* Grafana
* vmagent
* VictoriaMetrics
* Existing services
* User traffic
* Authentication flow
* Metrics flow
* Log/traffic flow

#### 3. Architecture decision

Explain why you selected the proposed Traefik/Keycloak placement.

#### 4. Required changes

List exactly what needs to change on each of the 3 machines.

Use:

* Machine 1
* Machine 2
* Machine 3

with concrete changes under each.

#### 5. Docker Compose

Provide production-ready Docker Compose configuration where appropriate.

Do NOT replace my existing Compose files blindly.

Show:

* New files
* Files that need modification
* Exact sections to add/change

Preserve existing configuration wherever possible.

#### 6. Traefik configuration

Provide complete configuration for:

* Static configuration
* Dynamic configuration
* Docker labels
* EntryPoints
* Routers
* Services
* Middlewares
* TLS
* Access logs
* Metrics
* Authentication

#### 7. Keycloak configuration

Provide:

* Realm design
* Client configuration
* Redirect URLs
* Roles/groups if required
* Admin configuration
* Database configuration
* Proxy settings
* HTTPS settings
* Integration with Traefik/authentication middleware

#### 8. Prometheus/vmagent configuration

Show exactly how my existing vmagent should scrape Traefik metrics on all 3 machines.

Use the existing configuration style from the README rather than inventing a completely different monitoring architecture.

#### 9. Grafana

Provide the PromQL queries required for the main traffic panels.

#### 10. Traffic dashboard

Design a Grafana dashboard showing:

* Traffic per hostname
* Traffic per machine
* Requests/sec
* Error rate
* Latency
* HTTP status distribution
* Backend/service traffic
* Active connections

#### 11. Access logs

Explain whether Traefik access logs should be:

* Parsed by Promtail/Loki
* Sent to OpenSearch
* Parsed by another existing component
* Or simply retained locally

Choose based on what already exists in the README.

#### 12. DNS

Provide the DNS records required for the proposed URLs.

#### 13. Firewall

Provide the exact firewall rules required.

#### 14. Testing

Give me a step-by-step validation procedure.

For example:

1. Start Traefik
2. Check Traefik logs
3. Check dashboard/API
4. Test Keycloak
5. Test Grafana
6. Authenticate through Keycloak
7. Generate traffic
8. Verify Traefik metrics
9. Verify vmagent scraping
10. Verify VictoriaMetrics ingestion
11. Verify Grafana panels

Include commands such as:

`curl`

`docker logs`

`docker inspect`

`docker network inspect`

and appropriate Prometheus/VictoriaMetrics queries.

#### 15. Failure testing

Show how to test:

* Machine 1 down
* Machine 2 down
* Machine 3 down
* Traefik down
* Keycloak down
* Grafana down
* VictoriaMetrics unavailable
* vmagent unavailable

and explain what should happen in each case.

### Critical instruction

Do not assume the README is correct.

If you find:

* Incorrect IPs
* Conflicting ports
* Duplicate services
* Hard-coded localhost references
* Incorrect Docker networks
* Security problems
* Single points of failure
* Incorrect Prometheus/vmagent configuration
* Existing reverse proxies
* Services already using ports required by Traefik

point them out before proposing the changes.

Also distinguish clearly between:

**CURRENT**
what I have today

**PROPOSED**
what you recommend changing

**OPTIONAL**
improvements that are not required

### Most important goal

I want to keep my existing monitoring environment working while adding:

**Keycloak authentication + Traefik reverse proxy + centralized traffic monitoring across all 3 machines.**

The final solution should be practical and implementable, not just an architecture diagram.

Start by reading and analyzing:

`D:\New-DATA-After-backup\grafana-key-vmagent-project\grafana-vmagent-keyclock-storage-nama.txt`

Do not skip this step.
